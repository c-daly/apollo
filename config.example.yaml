# Apollo Configuration Example
# Copy this file to config.yaml and customize as needed

sophia:
  host: localhost
  port: 8080
  timeout: 30
  api_key: ""  # Optional bearer token for Sophia API

hermes:
  host: localhost
  port: 8081
  timeout: 30
  api_key: ""  # Optional bearer token for Hermes API

persona_api:
  host: localhost
  port: 8082
  timeout: 15
  api_key: ""  # Optional bearer token if Apollo API is secured

hcg:
  neo4j:
    uri: bolt://localhost:7687
    user: neo4j
    password: password
  milvus:
    host: localhost
    port: 19530
